
const RenderHtmlApp = {
    data() {
        return {
            nazolist: toplist
        }
    },
    methods: {
    },
    mounted() {
    }
};
const app = Vue.createApp(RenderHtmlApp);
const vm = app.mount('#top');