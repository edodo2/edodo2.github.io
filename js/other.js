
const RenderHtmlApp = {
    data() {
        return {
            otherlist: olist
        }
    },
    methods: {
    },
    mounted() {
    }
};
const app = Vue.createApp(RenderHtmlApp);
const vm = app.mount('#other');